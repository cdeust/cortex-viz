# Bench manifest — cortex-viz v2.6.2

- Date (UTC): 2026-07-13T19:02:19Z
- Base commit: f9d1689 (pre-release HEAD)
- Change since v2.6.1: file detail panel — "See full diff" button under the
  inline git diff (ui/unified/js/trace_detail.js), opening the full GitHub-style
  diff modal via the existing .tool-diff-btn delegation (detail_diff.js).
- Non-regression substitute (cortex-viz has no CI/bench infra): full pytest suite
  on the exact release tree.

## Result

```
$ .venv/bin/python -m pytest -q
363 passed in 11.19s
```

- Exit code: 0
- Environment: macOS (darwin 25.5.0), Python 3.12, pytest 9.1.0, .venv
